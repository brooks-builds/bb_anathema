vstack
	@BBHeading [text: "Creating a Component"]
	text "Create a new module folder in the component library crate with a `mod.rs` and `template.aml` file in it"
	@BBBlock [value: state.rust_code, header: true, copy: true, scroll: true, max_height: 7]
	text "Wire up the new module in the component library lib.rs file"

